name: ci
on:
  pull_request:
  push:
    branches: [main]
jobs:
  ios:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: brew install xcodegen
      - run: scripts/ci.sh

