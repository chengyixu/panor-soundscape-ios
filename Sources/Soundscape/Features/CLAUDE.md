# Features Sector

Feature slices own `@MainActor @Observable` presentation state and SwiftUI composition. They depend on shared protocols, expose loading/empty/error states, and do not import concrete networking or Keychain adapters.

