# Contracts Sector

Canonical domain models, DTO mappings, API paths, repository protocols, and user-facing error taxonomy. This sector imports Foundation only and owns every client/server name exactly once.

